---
description: Publish local changes as an Azure DevOps PR with a linked work item
---

Load and execute the **publish-pr** skill (`skills/publish-pr/SKILL.md`). Follow its full workflow — analyze changes, create a work item, push the branch, create the PR, and optionally tend to feedback.

$ARGUMENTS
