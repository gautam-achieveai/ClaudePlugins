---
description: Publish local changes as an Azure DevOps PR with a linked work item
argument-hint: "[options or branch name]"
---

Load and execute the **ado:publish-pr** skill. Follow its full workflow — analyze changes, create a work item, push the branch, create the PR, and optionally tend to feedback.

$ARGUMENTS
