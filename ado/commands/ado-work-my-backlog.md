---
description: >
  Autonomously process all assigned Azure DevOps work items in the current sprint —
  classifies, plans, implements, and babysits PRs using parallel agents.
argument-hint: "[--dry-run]"
---

Load and execute the **ado:ado-work-my-backlog** skill (`skills/ado-work-my-backlog/SKILL.md`). Follow its full workflow — load state, classify work items, and process all stages using parallel agents where possible.

$ARGUMENTS
