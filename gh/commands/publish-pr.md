---
description: Publish local changes as a GitHub PR with a linked issue
argument-hint: "[options or branch name]"
---

Load and execute the **gh:publish-pr** skill. Follow its full workflow — analyze changes, create or link a GitHub issue, push the branch, create the PR, and optionally tend to feedback.

$ARGUMENTS
