---
description: >
  Autonomously process assigned GitHub work items (GitHub Issues + Projects) —
  classify, plan, implement, and babysit PRs using parallel agents.
argument-hint: "[--dry-run]"
---

Load and execute the **gh:work-my-backlog** skill (`skills/work-my-backlog/SKILL.md`). Follow its full workflow — load state, classify work items, and process all stages using parallel agents where possible.

$ARGUMENTS
